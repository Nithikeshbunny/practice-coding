#include <stdio.h>
int main()
{
int i,X;
printf("Enter number")
scan f("%d",&x)
for (i=2;i<=x i++)
{
if(x%i==0)\
{
printf("It is not prime number");
break;
}
}
if(x==i)
{
printf("It is prime number");
}
return 0;
